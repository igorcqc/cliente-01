/**
 * Constantes técnicas do motor — NÃO é conteúdo do cliente.
 * Edite este arquivo apenas para mudanças estruturais da base
 * (ex: largura máxima do container, nome interno do produto).
 */
export const siteConfig = {
  produto: "Bio que Vende",
  containerMaxWidth: "max-w-md",
} as const;
